spring:
  # jpa & hibernate tunning
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true
        "[highlight_sql]": true

#logging:
#  level:
#    org.springframework.web: DEBUG